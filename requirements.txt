# CML Serving Python Client
# NOTE: caiiclient must be installed separately before running pip install -r requirements.txt
# 
# Option 1: Download from your cluster
#   curl https://<DOMAIN>/api/v1alpha1/client/python --output caiiclient.tar.gz
#   pip install caiiclient.tar.gz
#
# Option 2: Generate locally (from repository root)
#   make build-api-pythonlib
#   pip install build/python-clients/out/caiiclient.tar.gz
#
# Option 3: Use the automated setup script
#   ./setup.sh
#
# Then install these dependencies:

# HTTP clients
httpx>=0.27.0
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0

